spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
  logging:
    level:
      org.flywaydb: DEBUG
      org.springframework: DEBUG

google:
  client-id: 1013805547853-gfkt7q0h0ctlqs8q138k0cg8pre03k8s.apps.googleusercontent.com
  client-secret: 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
  redirect-uri: http://localhost:8080/api/auth/google/callback